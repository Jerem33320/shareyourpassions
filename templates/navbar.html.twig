<div class="navbar navbar-expand-sm navbar-light">
    <a class="navbar-brand" href="{{ url('home') }}">
        <img src="{{ asset('img/logo.png') }}" />
    </a>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            {% if app.user is null %}
            <li class="nav-item">
                <a class="nav-link" href="{{ url('app_login') }}">Se Connecter</a>
            </li>
            {% endif %}
            {% if app.user is not null %}
            <li class="nav-item dropdown">
                <img class="avatar dropdown-toggle" id="navbarDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" src="{{ app.user.avatar is defined ? app.user.avatar : '' }}" />
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ url('user') }}">Mon Profil</a>
                    <a class="dropdown-item" href="{{ url('app_logout') }}">Se Déconnecter</a>
                </div>
            </li>
            {% endif %}
        </ul>
    </div>
</div>